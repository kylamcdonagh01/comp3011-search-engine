[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --strict-markers
    --tb=short
markers =
    integration: integration tests that exercise multiple modules
    slow: tests that are slow (deselect with '-m "not slow"')